# aidd-docs

社外向け「AI駆動開発」研修教材の開発リポジトリ。成果物は Markdown ドキュメント（将来スライド化）。

## 教材の方針

- **原則中心、事例として Claude Code**。原則の記述とツール固有の記述を混ぜない。
- 執筆はAI主導、構成の合意とレビューは人が行う。

## 基準となる文書

執筆・レビューの判断は、必ず次を根拠にする。ここに書かれていないことを根拠に判断しない。

| 文書 | 役割 |
|---|---|
| `foundation/audience.md` | 受講者像。すべての判断の基準 |
| `foundation/curriculum.md` | 学習目標と章立て |
| `foundation/style-guide.md` | 文体・表記・パラグラフ規約・frontmatter 仕様 |
| `foundation/glossary.md` | 用語の正式表記 |
| `foundation/review-criteria.md` | レビューのカテゴリ・重大度・書式 |

## ディレクトリ

```
docs/<NN>-<章スラッグ>/     教材本体。outline.md と <MM>-<節スラッグ>.md
foundation/                 上記の基準文書
sources/references.md       出典台帳
logs/review-log.md          レビュー指摘ログ（振り返りの入力）
retrospectives/             章ごとの振り返り
backlog.md                  タスク管理
.claude/agents/             レビュー用サブエージェント
.claude/commands/           ワークフローのコマンド
```

## ワークフロー

```
[1] カリキュラム設計      foundation/curriculum.md
 ↓
[2] アウトライン設計      /outline <章>     → 人の合意が必須
 ↓
[3] 執筆                 /draft <章-節>
 ↓
[3.5] 手順検証           /verify-demo <path>  → デモ手順を含む節のみ。実際に実行して確認
 ↓
[4] レビュー             /review-doc <path> → 5エージェント並列 + ログ記録 + 3回ルール判定
 ↓
[5] 指摘反映             fix/ ブランチ
 ↓
[6] 人間レビュー → マージ  backlog.md を更新
 ↓ 章完了
[R] 振り返り             /retro <章>        → Try を backlog へ
 ↓
[I] 改善適用             process/ ブランチ（別 PR）
```

## タスク管理

`backlog.md` に集約する。

| タイミング | 担当 | 操作 |
|---|---|---|
| 作業開始時 | AI | 該当タスクを In Progress に移し、ブランチを切る |
| PR 作成時 | AI | PR 本文にタスク名を書く |
| マージ | 人 | PR をマージし、マージした旨を伝える |
| **マージ直後** | **AI** | **チェックを付け、In Progress から外す。次の作業を始める前に行う** |

In Progress は1〜2件までにする。

## ブランチ命名

| 用途 | 命名 |
|---|---|
| カリキュラム設計（Phase 1） | `curriculum/<内容>` |
| アウトライン設計 | `outline/ch<NN>` |
| 執筆 | `draft/ch<NN>-<MM>` |
| レビュー指摘の反映 | `fix/ch<NN>-<MM>` |
| プロセス改善 | `process/<改善内容>` |
| 振り返り | `retro/ch<NN>` |

`main` に直接コミットしない。タスク単位でブランチを切り、PR でマージする。

## 応答の作法

レビューする側の負担を減らすことを優先する。**丁寧さと読みやすさは別である。**

- **結論と決め手を先に短く述べる。** 根拠・選択肢・波及論点をすべて一度に出さない。詳細は求められてから出す
- 判断を求めるときは、**選択肢を3つまで、各1〜2文**にする。推奨を明示する
- 波及して気づいた論点は、**1行で触れるに留める**。掘り下げるかは相手に委ねる
- 差分も小さく分ける。1つの PR で扱う変更は1つの主題に絞る

## 守るべき原則

1. **アウトラインの合意なしに執筆しない。** 合意がずれていれば全文が無駄になる。**この原則は `foundation/` の文書にも適用する。** 大きな文書を起案する前に、節構成と各節に書くことを合意する。
2. **出典のないツール固有の記述を書かない。** 出典には確認日を必ず添える。
3. **検証していない手順を教材に載せない。** 教材は研修後に一人で読まれる。動かない手順は受講者を止める。
4. **レビューは読むだけ。修正は別ブランチで行う。** 診断と変更を分ける。
5. **診断（`/retro`）と改善の適用（`process/`）を同じ PR に混ぜない。** ルールの理由が git 履歴から辿れなくなる。
6. **同じ指摘が3回出たら、個別に直すのをやめて仕組みで防ぐ。** 判定は `/review-doc` が自動で行う。
7. **未確定の基準文書を放置して先に進まない。** `audience.md` に未確定項目がある状態で執筆に入らない。

## 執筆時の頻出ルール

詳細は `foundation/style-guide.md`。特に守るもの。

- 1段落1トピック。段落の第1文を要約文にする
- H2 = スライド1枚分の主題。直下に主張文を1文置く
- ツール固有の記述は `> [!NOTE]` の囲みに入れ、出典 URL と確認日を書く
- 曖昧語（「など」「適切に」「基本的に」）を使わない
- 用語は `glossary.md` の正式表記に従う
